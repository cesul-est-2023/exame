class Carta {
  constructor(valor, naipe) {
    this.valor = valor;
    this.naipe = naipe;
  }
}

function ordernarPorValor(cartas) {}

function ordernarPorNaipe(cartas) {}

module.exports = { Carta, ordernarPorValor, ordernarPorNaipe };
